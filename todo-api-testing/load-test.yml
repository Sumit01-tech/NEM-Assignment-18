config:
  target: "http://localhost:3000"
  phases:
    - duration: 30
      arrivalRate: 10
scenarios:
  - flow:
      - post:
          url: "/todos"
          json:
            task: "Load Testing"
